name: modsen_practice
description: "A new Flutter project."

publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  firebase_core: any
  firebase_auth: any
  flutter_svg: ^2.2.0
  go_router: ^15.2.3
  flutter_bloc: ^9.1.1
  json_annotation: ^4.9.0
  dio: ^5.8.0+1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^5.0.0
  flutter_native_splash: ^2.4.6
  json_serializable: ^6.8.0
  build_runner: ^2.4.15

flutter:

  uses-material-design: true

  assets:
    - assets/svg/