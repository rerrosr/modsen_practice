import 'package:flutter/material.dart';

abstract class CryptoCoinColors {
  // Main background color
  static const Color blackBackgroundColor = Colors.black;
  static const Color greyBackgroundColor = Color.fromRGBO(158, 158, 158, 1);

  // Text colors
  static const Color whiteColor = Colors.white;
  static const Color white24Color = Colors.white24;
  static const Color whiteSecondaryColor = Colors.white70;
  static const Color greyTextColor = Color.fromRGBO(117, 117, 117, 1);

  // Button colors
  static const Color blueAccentColor = Colors.blueAccent;
  static const Color blueButtonColor = Color.fromRGBO(0, 87, 255, 1);
  static const Color blueButtonColorDark = Color.fromRGBO(38, 85, 255, 1);

  // SnackBar colors
  static const Color snackBarErrorColor = Colors.redAccent;
}
