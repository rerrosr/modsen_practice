import 'package:flutter/material.dart';

abstract class CryptoCoinColors {
  // Main background color
  static const Color blackBackgroundColor = Colors.black;

  // Text colors
  static const Color whiteColor = Colors.white;
  static const Color whiteSecondaryColor = Colors.white70;

  // Button colors
  static const Color blueButtonColor = Color.fromRGBO(0, 87, 255, 1);
}
